<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Challenge Requests</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        .challenge-card {
            margin: 10px 0;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: white;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Challenge Requests</h1>
        <% if (challenges.length > 0) { %>
            <% challenges.forEach(challenge => { %>
                <div class="challenge-card">
                    <h3><%= challenge.title %></h3>
                    <p><strong>Description:</strong> <%= challenge.description %></p>
                    <p><strong>Creator:</strong> <%= challenge.creator %></p>
                    <p><strong>Reward:</strong> <%= challenge.points %> pts</p>
                    <p><strong>Duration:</strong> <%= challenge.duration %> days</p>
                    <form action="/acceptChallenge/<%= challenge._id %>" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-success">Accept</button>
                    </form>
                    <form action="/declineChallenge/<%= challenge._id %>" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger">Decline</button>
                    </form>
                </div>
            <% }); %>
        <% } else { %>
            <p class="text-center">No challenge requests at the moment!</p>
        <% } %>
    </div>
</body>
</html>
