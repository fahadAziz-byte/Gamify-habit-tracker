<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Habitica Market</title>
    <style>
        :root {
            --primary-purple: #4f2a93;
            --dark-purple: #271B3D;
            --light-purple: #6133b4;
            --health-red: #f74e52;
            --exp-yellow: #ffb445;
            --light-bg: #e1e0e3;
            --border-color: #c3c0c7;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: var(--light-bg);
            min-height: 100vh;
        }

        /* Top Navigation */
        .main-nav {
            background-color: var(--primary-purple);
            padding: 0.8rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            color: white;
        }

        .nav-left {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-left a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: opacity 0.2s;
            font-size: 1rem;
        }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .currency {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background-color: #24cc8f;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            font-weight: 500;
        }

        /* Profile Section */
        .profile-section {
            background: var(--dark-purple);
            color: white;
            padding: 2rem;
            display: grid;
            grid-template-columns: auto 1fr auto;
            gap: 2rem;
            position: relative;
        }

        .avatar-frame {
            width: 140px;
            height: 140px;
            background-color: var(--dark-purple);
            border: 3px solid #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .avatar-frame img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* یا object-fit: contain اگر آپ تصویر کو کھینچنا نہیں چاہتے */
            border-radius: 50%; /* گول فریم کے لیے */
        }

        .profile-info {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .profile-info h2 {
            font-size: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .profile-info p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.9rem;
        }

        .progress-bars {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
            margin-top: 0.5rem;
        }

        .progress-bar {
            width: 100%;
            height: 20px;
            background: rgba(0, 0, 0, 0.3);
            border-radius: 10px;
            overflow: hidden;
            position: relative;
        }

        .health-bar {
            background: var(--health-red);
            height: 100%;
            width: 100%;
        }

        .exp-bar {
            background: var(--exp-yellow);
            height: 100%;
            width: 0%;
        }

        .progress-text {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            color: white;
            font-size: 0.9rem;
            text-shadow: 1px 1px 0 rgba(0, 0, 0, 0.5);
        }

        .play-card {
            background: rgba(0, 0, 0, 0.2);
            padding: 1.5rem;
            border-radius: 8px;
            max-width: 300px;
        }

        .play-card h3 {
            color: white;
            margin-bottom: 1rem;
        }

        .play-card p {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.9rem;
            line-height: 1.4;
            margin-bottom: 1rem;
        }

        /* Market Navigation */
        .market-nav {
            background-color: white;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            gap: 1rem;
            padding: 0 2rem;
        }

        .market-nav a {
            padding: 1rem 1.5rem;
            text-decoration: none;
            color: #4e4a57;
            position: relative;
            font-weight: 500;
        }

        .market-nav a.active {
            color: var(--primary-purple);
            border-bottom: 3px solid var(--primary-purple);
        }

        /* Market Content */
        .market-content {
            background-image: url('/images/stone-wall.png');
            background-repeat: repeat;
            min-height: calc(100vh - 280px);
            padding: 2rem;
        }

        .market-container {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 250px 1fr;
            gap: 2rem;
        }

        /* Sidebar */
        .sidebar {
            background: white;
            padding: 1.5rem;
            border-radius: 8px;
            border: 1px solid var(--border-color);
        }

        .search-box {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            margin-bottom: 1.5rem;
        }

        .filters h2 {
            margin-bottom: 1rem;
            font-size: 1.1rem;
            color: var(--dark-purple);
        }

        .filter-option {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 0.75rem;
        }

        /* Items Grid */
        .items-section {
            background: white;
            border-radius: 8px;
            border: 1px solid var(--border-color);
            padding: 1.5rem;
        }

        .featured-banner {
            background: #b27d00;
            color: white;
            padding: 0.75rem 1rem;
            border-radius: 4px;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .items-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1.5rem;
        }

        .item-card {
            background: white;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1rem;
            text-align: center;
            position: relative;
        }

        .item-image {
            width: 80px;
            height: 80px;
            margin: 0 auto 1rem;
            object-fit: contain;
        }

        .item-card h3 {
            color: var(--dark-purple);
            margin-bottom: 0.5rem;
            font-size: 1rem;
        }

        .item-card p {
            color: #666;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .cost {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.25rem;
            color: #b27d00;
            font-weight: 500;
            margin: 0.5rem 0;
        }

        button {
            width: 100%;
            padding: 0.75rem;
            border: none;
            border-radius: 4px;
            background: var(--primary-purple);
            color: white;
            cursor: pointer;
            font-weight: 500;
            transition: background 0.2s;
        }

        button:hover:not(:disabled) {
            background: var(--light-purple);
        }

        button:disabled {
            background: #c3c0c7;
            cursor: not-allowed;
        }

        @media (max-width: 1024px) {
            .market-container {
                grid-template-columns: 1fr;
            }

            .sidebar {
                display: none;
            }
        }

        @media (max-width: 768px) {
            .profile-section {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .avatar-frame {
                margin: 0 auto;
            }

            .play-card {
                margin: 0 auto;
            }

            .main-nav {
                padding: 0.8rem 1rem;
            }

            .nav-left {
                gap: 1rem;
            }

            .market-content {
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <nav class="main-nav">
        <div class="nav-left">
            <img src="/placeholder.svg?height=30&width=30" alt="Habitica Logo">
            <a href="/habits">Tasks</a>
            <a href="/inventory">Inventory</a>
            <a href="/shop" class="active">Shops</a>
            <a href="/challenges">Challenges</a>
            <a href="/leaderboard">Leaderboard</a>
        </div>
        <div class="nav-right">
            <div class="currency">
                <span><%= user.coins %></span>🪙
            </div>
        </div>
    </nav>

    <section class="profile-section">
        <div class="avatar-frame">
            <%if(userAvatar.imageURL){%>
                <img src="/<%= userAvatar.imageURL %>" alt="Character Avatar">
            <% }else{ %>
                <img src="/placeholder.svg?height=100&width=100" alt="Character Avatar">
            <% } %>
        </div>
        <div class="profile-info">
            <h2>
                <%= user.username %>
                <span class="level">Level 1 Warrior</span>
            </h2>
            <div class="progress-bars">
                <div class="progress-bar">
                    <div class="health-bar"></div>
                    <span class="progress-text">50 / 50</span>
                </div>
                <div class="progress-bar">
                    <div class="exp-bar"></div>
                    <span class="progress-text">0 / 25</span>
                </div>
            </div>
        </div>
        <div class="play-card">
            <h3>Play Habitica with Others</h3>
            <p>Start your own Party or join an existing one to take on Quests and boost your motivation!</p>
        </div>
    </section>

    <nav class="market-nav">
        <a href="/shop" >Market</a>
        <a href="/inventory" class="active">Inventory</a>
    </nav>

    <main class="market-content">
        <div class="market-container">
            <aside class="sidebar">
                <input type="text" class="search-box" placeholder="Search">
                <div class="filters">
                    <h2>Filters</h2>
                    <div class="filter-option">
                        <input type="checkbox" id="equipment">
                        <label for="equipment">Equipment</label>
                    </div>
                    <div class="filter-option">
                        <input type="checkbox" id="eggs">
                        <label for="eggs">Eggs</label>
                    </div>
                    <div class="filter-option">
                        <input type="checkbox" id="hatching">
                        <label for="hatching">Hatching Potions</label>
                    </div>
                </div>
            </aside>

            <section class="items-section">
                <div class="featured-banner">
                    ⭐ Featured Items
                </div>
                
                <div class="items-grid">
                    <% potions.forEach((potion) => { 

                        if(potion.activatedAt){ %>
                            <div class="item-card">
                                <img class="item-image" src="/<%= potion.imageURL %>" alt="<%= potion.name %>">
                                <h3><%= potion.name %></h3>
                                <p><%= potion.description %></p>
                                <p>Cost: <%= potion.cost %> coins</p>
                                <form action="/activatePotion/<%= potion.potionId %>"  method="post">
                                    <button type="submit" disabled>Already Activated</button>
                                </form>
                            </div>
                        <% }else{ %>
                            <div class="item-card">
                                <img class="item-image" src="/<%= potion.imageURL %>" alt="<%= potion.name %>">
                                <h3><%= potion.name %></h3>
                                <p><%= potion.description %></p>
                                <p>Cost: <%= potion.cost %> coins</p>
                                <form action="/activatePotion/<%= potion.potionId %>"  method="post">
                                    <button type="submit">Activate Potion</button>
                                </form>
                            </div>
                        <% }
                    }); %>
                </div>
            </section>
        </div>
    </main>
</body>
</html>